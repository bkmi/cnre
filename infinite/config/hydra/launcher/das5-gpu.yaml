defaults:
  - submitit_slurm

timeout_min: 60
cpus_per_task: 8
gpus_per_node: 1
nodes: 1
partition: defq
constraint: TitanX
exclude: null
signal_delay_s: 120
max_num_timeout: 0
gres: gpu:${hydra.launcher.gpus_per_node}
array_parallelism: 16
setup:
  - conda activate ./env
